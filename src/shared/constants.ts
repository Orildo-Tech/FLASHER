// Allowed image formats
export const SUPPORTED_EXTENSIONS = ['iso', 'img', 'raw'];

// Default buffer chunk
export const DEFAULT_CHUNK_SIZE = 1024 * 1024 * 2;

// Legacy buffer size
export const CHUNK_SIZE_USB2 = 1024 * 1024 * 2;

// Fast buffer size
export const CHUNK_SIZE_USB3 = 1024 * 1024 * 4;

// Maximum buffer size
export const CHUNK_SIZE_MAX = 1024 * 1024 * 8;

// Format chunk size
export const FULL_FORMAT_CHUNK = 1024 * 1024 * 4;
